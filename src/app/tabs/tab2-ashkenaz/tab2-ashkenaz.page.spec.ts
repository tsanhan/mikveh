import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab2AshkenazPage } from './tab2-ashkenaz.page';

describe('Tab2AshkenazPage', () => {
  let component: Tab2AshkenazPage;
  let fixture: ComponentFixture<Tab2AshkenazPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab2AshkenazPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
