import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tab3SfardPage } from './tab3-sfard.page';

describe('Tab3SfardPage', () => {
  let component: Tab3SfardPage;
  let fixture: ComponentFixture<Tab3SfardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tab3SfardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
