import { TranslateHebPipe } from './translate-heb.pipe';

describe('TranslateHebPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateHebPipe();
    expect(pipe).toBeTruthy();
  });
});
